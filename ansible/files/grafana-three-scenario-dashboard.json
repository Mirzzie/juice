{
  "dashboard": {
    "title": "Thesis: IAST vs RASP (Industry Benchmarks)",
    "panels": [
      {
        "id": 1,
        "title": "🏆 THESIS SUMMARY: Detection vs Prevention",
        "type": "table",
        "gridPos": { "h": 8, "w": 24, "x": 0, "y": 0 },
        "targets": [
          { "expr": "sum by (scenario) (increase(thesis_rasp_blocks_total[24h]))", "legendFormat": "Blocks", "refId": "A" },
          { "expr": "sum by (scenario) (increase(thesis_iast_detections_total[24h]))", "legendFormat": "IAST Findings", "refId": "B" }
        ]
      },
      {
        "id": 2,
        "title": "🛡️ Real-Time Block Rate (Current Scenario)",
        "type": "gauge",
        "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
        "targets": [
          { "expr": "(sum(increase(thesis_rasp_blocks_total[5m])) / (sum(increase(thesis_rasp_detections_total[5m])) + 0.001)) * 100", "legendFormat": "Block %" }
        ]
      },
      {
        "id": 3,
        "title": "⚡ Performance Overhead (Latency)",
        "type": "timeseries",
        "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
        "targets": [
          { "expr": "rate(juice_shop_http_server_request_duration_seconds_sum[1m]) / rate(juice_shop_http_server_request_duration_seconds_count[1m]) * 1000", "legendFormat": "Avg Latency (ms)" }
        ]
      },
      {
        "id": 4,
        "title": "🔍 Vulnerabilities Detected by Type (IAST)",
        "type": "bargauge",
        "gridPos": { "h": 8, "w": 24, "x": 0, "y": 16 },
        "targets": [
          { "expr": "sum by (vuln_type) (thesis_iast_detections_total)", "legendFormat": "{{vuln_type}}" }
        ]
      }
    ],
    "schemaVersion": 36,
    "version": 1,
    "uid": "thesis-main"
  }
}